{
  "name": "sqa-reporter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Turns a Patrol run into an Allure report. Named for what it is rather than what it emits: the markers it reads are Patrol's, and the format it writes is one choice among several. Provides the Allure CLI; the pipeline itself lives in the Melos scripts (allureWeb, allureAndroid) so there is one place that knows the paths.",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=22",
    "pnpm": "11.0.8"
  },
  "devDependencies": {
    "allure": "3.14.3"
  }
}
