name: design_system
description: "Design tokens, light/dark themes and primitive widgets for the market workspace."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  cupertino_icons: 1.0.8
  flutter:
    sdk: flutter
  shimmer: 3.0.0

dev_dependencies:
  lint: 0.1.0
