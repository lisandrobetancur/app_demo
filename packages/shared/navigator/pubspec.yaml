name: navigator
description: "Navigation layer for the market workspace: AppRoute contract and NavigationManager over go_router."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: 2.6.1
  go_router: 17.3.0

dev_dependencies:
  lint: 0.1.0
