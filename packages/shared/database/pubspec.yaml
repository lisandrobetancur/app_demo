name: database
description: "SQLite layer for the market workspace: connection, schema, migrations, deterministic seed and base DAO."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: 2.6.1
  path: 1.9.1
  sqflite: 2.4.2
  sqflite_common_ffi_web: 1.1.1
  utilities: 0.1.0

dev_dependencies:
  lint: 0.1.0
