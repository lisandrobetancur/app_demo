{
  "version": 1,
  "description": "Index of the test data. Each entry under \"datasets\" is a logical name and the file holding it; adding a data set is one line here and a file beside it. The loader knows no name in advance: it reads whatever this file lists.",
  "datasets": {
    "users": "users.json",
    "products": "products.json",
    "coupons": "coupons.json",
    "seed": "seed.json",
    "invalid_logins": "invalid_logins.json"
  }
}
